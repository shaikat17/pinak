import { A11y, Navigation, Pagination } from "swiper";
import "swiper/css";
import "swiper/css/pagination";
import { Swiper, SwiperSlide } from "swiper/react";
// import { SwiperNavButtons } from "./SwiperNabButtons";

const TeamMemberSlider = () => {
  return (
    <div className="px-20 w-full h-full overflow-x-hidden relative bg-red-500 text-white text-5xl text-center">
        Slider section
    </div>
  );
};

export default TeamMemberSlider;
